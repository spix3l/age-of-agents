---
id: D8-07
title: "Vary the opponent's opening between matches"
epic: EPIC-08
day: 8
priority: P1
estimate: M
status: DONE
owner: claude-opus-5
depends_on: ["D8-02"]
---

# D8-07 — Vary the opponent's opening between matches

[Back to EPIC-08](../epics/EPIC-08-depth.md) · [Backlog index](../../BACKLOG.md)

**Outcome:** Two matches against the same difficulty open differently, so the player reads the game instead of reciting it.

**Owns:** `src/game/ai/`, `src/data/ai.ts`.

**Acceptance:** the opponent picks an opening plan from the match seed, and the plans differ materially in build order, when and how it scouts, what it produces first, and how early it commits; the observed pattern "worker scouts, then a drone, then one army" is no longer the only sequence; every plan is viable — none loses to itself or stalls; the choice is reproducible for a fixed seed; difficulty still dominates plan, so relentless is always harder than relaxed.

**Verify:** a test asserting distinct opening fingerprints across seeds, plus re-measured soaks on every difficulty.

## Completion checklist

- [x] Dependencies are `DONE`.
- [x] Acceptance criteria above are satisfied.
- [x] Verification above passes alongside relevant existing checks.
- [x] `PROJECT_STATUS.md` is updated.
- [x] `AGENT_HANDOFF.md` is updated if architecture or operating instructions changed.
- [x] Frontmatter status is set to `DONE`; owner and material decisions are recorded.

## Outcome

Five seeded opening plans varying worker target, attack timing and force, build order, scouting, and unit mix; scouting patience stops every opening sending a Worker first.
